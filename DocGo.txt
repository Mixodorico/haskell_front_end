The Language go
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of go==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




CIdent literals are recognized by the regular expression
`````(letter | '_') (letter | digit | '_')*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in go are the following:
  | ``bool`` | ``break`` | ``catch`` | ``char``
  | ``continue`` | ``else`` | ``false`` | ``float``
  | ``for`` | ``func`` | ``if`` | ``int``
  | ``package`` | ``read`` | ``return`` | ``string``
  | ``true`` | ``try`` | ``var`` | ``void``
  | ``write`` |  |  | 

The symbols used in go are the following:
  | ( | ) | || | &&
  | ! | == | != | <
  | <= | > | >= | +
  | - | * | / | %
  | & | ++ | -- | [
  | ] | = | := | *=
  | += | /= | -= | {
  | } | ; | , | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of go==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Boolean// | -> | ``true`` 
  |  |  **|**  | ``false`` 
  | //RExp// | -> | //RExp1// 
  |  |  **|**  | //RExp// ``||`` //RExp1// 
  | //RExp1// | -> | //RExp2// 
  |  |  **|**  | //RExp1// ``&&`` //RExp2// 
  | //RExp2// | -> | //RExp3// 
  |  |  **|**  | ``!`` //RExp3// 
  | //RExp3// | -> | //RExp4// 
  | //RExp4// | -> | //RExp5// 
  | //RExp5// | -> | //RExp6// 
  |  |  **|**  | //RExp6// ``==`` //RExp6// 
  |  |  **|**  | //RExp6// ``!=`` //RExp6// 
  |  |  **|**  | //RExp6// ``<`` //RExp6// 
  |  |  **|**  | //RExp6// ``<=`` //RExp6// 
  |  |  **|**  | //RExp6// ``>`` //RExp6// 
  |  |  **|**  | //RExp6// ``>=`` //RExp6// 
  | //RExp6// | -> | //RExp7// 
  | //RExp7// | -> | //RExp8// 
  |  |  **|**  | //RExp7// ``+`` //RExp8// 
  |  |  **|**  | //RExp7// ``-`` //RExp8// 
  | //RExp8// | -> | //RExp9// 
  |  |  **|**  | //RExp8// ``*`` //RExp9// 
  |  |  **|**  | //RExp8// ``/`` //RExp9// 
  |  |  **|**  | //RExp8// ``%`` //RExp9// 
  | //RExp9// | -> | //RExp10// 
  | //RExp10// | -> | //RExp11// 
  |  |  **|**  | ``-`` //RExp11// 
  |  |  **|**  | ``&`` //LExp// 
  | //RExp11// | -> | //RExp12// 
  |  |  **|**  | //FunCall// 
  | //RExp12// | -> | //RExp13// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //Char// 
  |  |  **|**  | //String// 
  |  |  **|**  | //Boolean// 
  | //RExp13// | -> | ``(`` //RExp// ``)`` 
  |  |  **|**  | //LExp// 
  | //LExp// | -> | //LExp1// 
  |  |  **|**  | ``*`` //RExp// 
  | //LExp1// | -> | //LExp2// 
  |  |  **|**  | //LExp2// ``++`` 
  |  |  **|**  | //LExp2// ``--`` 
  | //LExp2// | -> | ``(`` //LExp// ``)`` 
  |  |  **|**  | //BLexp// 
  | //RExp14// | -> | ``(`` //RExp// ``)`` 
  | //FunCall// | -> | //CIdent// ``(`` ``)`` 
  |  |  **|**  | //CIdent// ``(`` //[RExp]// ``)`` 
  | //BLexp// | -> | //BLexp// ``[`` //RExp// ``]`` 
  |  |  **|**  | //CIdent// 
  | //Program// | -> | ``package`` //CIdent// //[Decl]// 
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// //[Decl]// 
  | //BasicType// | -> | ``void`` 
  |  |  **|**  | ``bool`` 
  |  |  **|**  | ``char`` 
  |  |  **|**  | ``float`` 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``string`` 
  |  |  **|**  | ``[`` //Integer// ``]`` //BasicType// 
  |  |  **|**  | ``*`` //BasicType// 
  | //Decl// | -> | ``var`` //[CIdent]// //BasicType// 
  |  |  **|**  | ``var`` //[CIdent]// ``=`` //[RExp]// 
  |  |  **|**  | ``var`` //[CIdent]// //BasicType// ``=`` //[RExp]// 
  |  |  **|**  | ``func`` //CIdent// ``(`` //[Param]// ``)`` //BasicType// //Block// 
  |  |  **|**  | ``func`` //CIdent// ``(`` //[Param]// ``)`` //Block// 
  | //ShortVarDecl// | -> | //[CIdent]// ``:=`` //[RExp]// 
  | //Assignment_op// | -> | ``=`` 
  |  |  **|**  | ``*=`` 
  |  |  **|**  | ``+=`` 
  |  |  **|**  | ``/=`` 
  |  |  **|**  | ``-=`` 
  | //Param// | -> | //[CIdent]// //BasicType// 
  | //StatementSmpl// | -> | //ShortVarDecl// 
  |  |  **|**  | //RExp// 
  |  |  **|**  | //LExp// //Assignment_op// //RExp// 
  | //Block// | -> | ``{`` //[Statement]// ``}`` 
  | //Statement// | -> | //Block// 
  |  |  **|**  | //StatementSmpl// 
  |  |  **|**  | ``return`` //RExp// 
  |  |  **|**  | ``if`` //RExp// //Block// 
  |  |  **|**  | ``if`` //RExp// //Block// ``else`` //Block// 
  |  |  **|**  | ``if`` //StatementSmpl// ``;`` //RExp// //Block// 
  |  |  **|**  | ``if`` //StatementSmpl// ``;`` //RExp// //Block// ``else`` //Block// 
  |  |  **|**  | ``for`` //[StatementSmpl]// ``;`` //RExp// ``;`` //[StatementSmpl]// //Block// 
  |  |  **|**  | ``for`` //RExp// //Block// 
  |  |  **|**  | //Decl// 
  |  |  **|**  | ``break`` 
  |  |  **|**  | ``continue`` 
  |  |  **|**  | ``try`` //Block// ``catch`` //Block// 
  |  |  **|**  | ``write`` ``(`` //RExp// ``)`` 
  |  |  **|**  | ``read`` ``(`` //RExp// ``)`` 
  | //[StatementSmpl]// | -> | **eps**
  |  |  **|**  | //StatementSmpl// 
  | //[CIdent]// | -> | //CIdent// 
  |  |  **|**  | //CIdent// ``,`` //[CIdent]// 
  | //[RExp]// | -> | //RExp// 
  |  |  **|**  | //RExp// ``,`` //[RExp]// 
  | //[Param]// | -> | **eps**
  |  |  **|**  | //Param// 
  |  |  **|**  | //Param// ``,`` //[Param]// 
  | //[Statement]// | -> | **eps**
  |  |  **|**  | //Statement// //[Statement]// 


